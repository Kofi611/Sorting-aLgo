#include<stdio.h>
#include<stdlib.h>

int main(int argc, char **argv){
	char * inpstr = argv[1];
	char * sorted = (char *) malloc(sizeof(char) * sizeof(argv[1]));
	char * temp = ;
	while(inpstr){
		
	}
	return 0;
}